<%= form_with(model: [ @portal, @portal.versions.build ]) do |f| %>
  <% if @portal.versions.build.errors.any? %>
    <div id="error_explanation">
      <h5>
        <%= pluralize(@portal.versions.build.errors.count, "error") %> prohibited
        the version from being saved:
      </h5>
      <ul>
        <%= @portal.versions.build.errors.full_messages.each do |msg| %>
          <li class="error"><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :number, "Version Number" %>
    <%= f.number_field :number %>
  </p>
  <p>

  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>