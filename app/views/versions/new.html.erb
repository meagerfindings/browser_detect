<h1>Add a New Version</h1>

<%= form_with(model: @version, url: portal_versions_path) do |f| %>
  <% if @version.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@version.errors.count, "error") %> prohibited
        the version from being saved:
      </h2>
      <ul>
        <%= @version.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :number, "Version Number" %>
    <%= f.number_field :number, :required => true %>
  </p>
  <p>



  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>